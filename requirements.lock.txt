# Candidate dependency pins validated by a clean CPython 3.12 environment.
# Install with: python -m pip install -r requirements.lock.txt
# Audit scope: CPython 3.12 on Windows. Regenerate and re-audit this lock for a
# different platform or interpreter generation. Pytest dependencies that apply
# only below Python 3.11 (`exceptiongroup`, `tomli`) are not represented here.

fastapi==0.139.0
uvicorn==0.50.0
httpx==0.28.1
websockets==15.0.1
PyYAML==6.0.3
python-multipart==0.0.32
pytest==8.4.0

# Transitive dependencies
starlette==0.52.1
pydantic==2.12.3
pydantic_core==2.41.4
annotated-types==0.7.0
typing_extensions==4.16.0
anyio==4.14.1
h11==0.16.0
httpcore==1.0.9
certifi==2026.6.17
idna==3.18
click==8.4.2
colorama==0.4.6; sys_platform == "win32"

# Required transitive packages confirmed by exact installed package metadata during
# the audit (FastAPI/Pydantic/Pytest dependency edges; versions may be platform
# or interpreter conditional where the metadata says so).
annotated-doc==0.0.5
typing-inspection==0.4.4
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.20.0
